# v3.7.0 Static Validation

## Result

Project **v3.7.0** / test suite **v3.1.0** completed the current static/CLI group cleanly on 2026-08-19:

```text
Passed    : 74
Failed    : 0
Skipped   : 0
Anomalies : 0
```

The validation command was:

```sh
sh tests/groups/00-static-cli.sh --run --verbose
```

## Scope

The static group validates all **81 public commands**, including:

- POSIX `/bin/sh` syntax and prohibited-Bash checks;
- standalone public-command packaging;
- lifecycle/elevation contracts;
- argument-taking function documentation;
- embedded companion payload synchronization;
- exact live `--help` / `.usage` synchronization;
- exact helper-document embedded-help synchronization;
- README/helper-index/test-matrix inventory coverage;
- all five help aliases: `-h`, `-?`, `/h`, `/?`, `--help`;
- usage output when required positional arguments are incomplete;
- descriptive `USAGE` and `DESCRIPTION` blocks;
- parsed long-option/help agreement;
- one exact standardized dry-run explanation;
- dry-run/version ordering and immutability contracts;
- physical LV identity and UUID-owned cleanup contracts;
- inactive activation-state restoration;
- archive/import integrity and remote-transfer verification contracts;
- exact mount ownership/source verification;
- single-drive/all-drive VM mount-engine source identity;
- the pre-existing overwrite rollback, cleanup and protected-state safety contracts.

## Real-host boundary

The current integration definition contains **137 real cases across 14 operational groups**.

Those cases require a disposable real Proxmox/LVM environment and were not executed as part of this static validation. In particular, the renamed/consolidated mount family changes public entry points and VM-level mount behavior, so v3.7.0 requires a fresh full real-host run before being described as integration-validated.

The last completed clean real-Proxmox acceptance baseline remains **v3.5.1 / test suite v2.9.1**. v3.7.0 is therefore **release-candidate / statically validated** at this stage.


## Subsequent real-host result

A full v3.7.0 / suite v3.1.0 run was completed on 2026-08-20 after this static validation. It passed 130/137 real integration cases with zero protected-state anomalies. The seven failed cases and their v3.7.1 corrective actions are documented in [`V3.7.0-REAL-INTEGRATION-RESULTS.md`](V3.7.0-REAL-INTEGRATION-RESULTS.md).
