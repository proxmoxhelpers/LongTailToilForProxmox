# v3.2.2 Static Validation

Date: 2026-08-17

v3.2.2 fixes the overwrite identity/rollback failure exposed by a real Proxmox run.

Local acceptance:

```text
Project commands                 : 40
Shell files parsed with /bin/sh  : 54
Syntax failures                  : 0
Standalone helpers               : 40/40
Test-matrix coverage             : 40/40
README wget+chmod entries        : 40
Plan-only test-group failures    : 0

Static suite:
Passed                           : 17
Failed                           : 0
Skipped                          : 0
Anomalies                        : 0
```

The new static regression requires both overwrite helpers to:

- retain a saved `NEW_UUID`;
- verify Proxmox storage mappings by LV UUID;
- locate displaced LVs by UUID during rollback;
- use config-only unused-reference helpers;
- track rollback failure and preserve remaining replacement LVs;
- contain no `qm set "$DEST_VM" --delete "$OLD_UNUSED_KEY"` transaction path.

This does not replace real Proxmox acceptance. Run the focused group first:

```sh
./tests/groups/50-copy-snapshot.sh --run --verbose
```

then the complete suite:

```sh
./tests/run-all-tests.sh --run --verbose
```
