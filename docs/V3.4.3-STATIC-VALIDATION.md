# v3.4.3 Static Validation

Date: 2026-08-17

Local validation after triaging the first v3.4.2 real-host run:

```text
Public commands                  : 42
Shell files parsed with /bin/sh  : 56
Static tests passed              : 40
Static tests failed              : 0
Static tests skipped             : 0
Static anomalies                 : 0
```

The six new v3.4.3 regressions cover:

- config-only source `unusedN` removal in `move-disk-to-vm.sh`;
- util-linux-compatible `partx` invocation;
- no undefined `trim` dependency in integration groups;
- non-zero destructive cancellation from `delete-lvm.sh`;
- SATA/IDE `iothread` sanitization in `change-disk-bus.sh`;
- protected after-state comparison during emergency/setup-failure cleanup.

The existing standalone, POSIX, dry-run, overwrite, vm/base, selector, documentation and harness-safety contracts also remain green.

This validation is static/local only. The corrected 84-case real Proxmox suite must be rerun before v3.4.3 can become the integration-validated baseline.
