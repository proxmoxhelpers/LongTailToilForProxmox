# v3.4.2 Static Validation

Date: 2026-08-17

Local validation of the v3.4.2 test/harness audit completed successfully.

```text
Public commands                  : 42
Shell files parsed with /bin/sh  : 56
Project-version failures         : 0
--help failures                  : 0
Plan-only group/runner failures  : 0

Static suite:
Passed                           : 34
Failed                           : 0
Skipped                          : 0
Anomalies                        : 0
```

The 34 static contracts include the existing POSIX/standalone/dry-run/overwrite/base-volume checks plus new tests for:

- cleanup refusal when a test VM points at unowned storage;
- dry-run snapshots including guest state and LV content samples;
- non-sparse regular-LV copy behavior;
- refusal helpers proving exact state unchanged;
- every public command having a non-static integration reference;
- every mutating command having a dry-run immutability case;
- README/test-matrix inventory matching all 42 public commands exactly once;
- exact-identity and owned-storage checks for LXC cleanup;
- protected baseline coverage of guest runtime/firewall state;
- fail-closed cleanup between guest, storage and VG/loop layers.

All ten group launchers and `tests/run-all-tests.sh` also complete successfully in plan-only mode.

This does not replace the real Proxmox integration run:

```sh
./tests/run-all-tests.sh --run --verbose
```
