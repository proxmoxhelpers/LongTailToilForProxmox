# v3.7.1 Real-Proxmox Integration Results

## Status

Project **v3.7.1** / test suite **v3.1.1** completed a full real-Proxmox all-groups run on **2026-08-22**.

The supplied console transcript reports:

```text
Static/CLI:        78 passed, 0 failed, 0 skipped, 0 anomalies
Real integration: 137 passed, 0 failed, 0 skipped, 0 anomalies
Operational cases: 137 total across 14 groups
Groups:            15 passed, 0 failed
```

The 15 groups are the static/CLI group plus all 14 registered operational groups. This satisfies the project release gate: every registered real case passed, no protected-state anomaly was reported, no fixture group aborted, and the all-groups runner completed with zero failed groups.

Project v3.7.1 is therefore designated **real-host integration-validated** for the covered Proxmox/LVM test matrix.

## Group results

| Group | Passed | Failed | Skipped | Anomalies | Result directory |
|---|---:|---:|---:|---:|---|
| `00-static-cli.sh` | 78 | 0 | 0 | 0 | `/tmp/proxmox-lvm-tools-v3-static.3hE6rE` |
| `10-inspection.sh` | 7 | 0 | 0 | 0 | `inspection-20260822184102-4142130` |
| `15-qol-inspection.sh` | 11 | 0 | 0 | 0 | `qol-inspection-20260822184226-4147799` |
| `20-lvm.sh` | 9 | 0 | 0 | 0 | `lvm-20260822184319-4150359` |
| `25-qol-lvm.sh` | 11 | 0 | 0 | 0 | `qol-lvm-20260822184341-4154497` |
| `30-mount.sh` | 6 | 0 | 0 | 0 | `mount-20260822184452-4159995` |
| `40-disk-lifecycle.sh` | 15 | 0 | 0 | 0 | `disk-lifecycle-20260822184534-4163182` |
| `50-copy-snapshot.sh` | 21 | 0 | 0 | 0 | `copy-snapshot-20260822185543-4182271` |
| `60-disk-config.sh` | 16 | 0 | 0 | 0 | `disk-config-20260822191420-31085` |
| `65-qol-disk-config.sh` | 9 | 0 | 0 | 0 | `qol-disk-config-20260822191942-50464` |
| `70-storage-io.sh` | 6 | 0 | 0 | 0 | `storage-io-20260822192236-57551` |
| `80-vm-config.sh` | 9 | 0 | 0 | 0 | `vm-config-20260822192448-62143` |
| `85-qol-workflows.sh` | 8 | 0 | 0 | 0 | `qol-workflows-20260822192753-68102` |
| `90-network.sh` | 3 | 0 | 0 | 0 | `network-20260822193034-74933` |
| `95-vm-archive.sh` | 6 | 0 | 0 | 0 | `vm-archive-20260822193110-76643` |

The operational counts sum to **137/137**.

## Exact source/static provenance

The supplied v3.7.1 candidate archive is:

```text
cae9c17166d3d05ece802d95b03abbc452270e214682c2cfa766e11bb6b051d8  LongTailToilForProxmox-v3.7.1-github-ready.zip
```

The supplied static evidence archive is:

```text
33f36443155b1b47767ef9c1929391cf8ca8d08f09d67071c361608c9e0822e1  proxmox-lvm-tools-v3-static.3hE6rE.zip
```

Its `environment.txt` identifies project **3.7.1**, test suite **3.1.1**, host `proxmox`, kernel `6.8.12-15-pve`, and capture time `2026-08-22T22:40:50Z`. Its `project-sha256.txt` contains all **342 files** in the v3.7.1 candidate tree, and every recorded file hash matches that candidate byte-for-byte.

That static result directory is the first group in the supplied `./tests/run-all-tests.sh --run --verbose` transcript, which then continues through all 14 operational groups.

## Integration-evidence attachment note

The file named `proxmox-lvm-tools-test-results.zip` that accompanied this review is **not** the evidence archive from the successful run above. Its SHA-256 is:

```text
8b8b179f6ef997b15cbe72f5e8621a35408abdeb1338eac7c32635a89336b83d  proxmox-lvm-tools-test-results.zip
```

Its result directories are from an earlier **v3.7.0 / suite v3.1.0** run around 01:29-02:19 on 2026-08-22, and its own summaries include failures in `qol-workflows` and `vm-archive`. It is therefore explicitly excluded from v3.7.1 acceptance evidence.

The v3.7.1 acceptance claim above is based on the complete successful all-groups console transcript plus the exact matching v3.7.1 static evidence archive. Because the current successful operational result directories were not supplied as an archive in this review, protected baseline/after files were not independently recounted pair-by-pair here; the all-groups transcript itself reports **0 anomalies in every operational group**.

## Corrective regressions confirmed on real Proxmox

The cases that directly cover the v3.7.0 failures all pass in the v3.7.1 run:

- `copy-vm-disk-options.sh preserves destination backing volume` passes after semantic option-order verification.
- `clone-vm-storage-only.sh copies active source disks` passes using regular raw staging and imported-byte verification.
- `clone-vm-storage-only.sh restores inactive base source activation` passes and returns the source LV to its prior activation state.
- `fix-vm-volume-names.sh leaves unrelated unmanaged LVM names unchanged` passes with a Proxmox-valid fixture.
- `import-vm.sh exact same-VMID restore reproduces config and disk content` passes while permitting a regenerated QEMU `vmgenid`.
- `send-vm-export-and-restore.sh transfer/restore dry-run is immutable` passes with no remote mutation path.
- The VM archive group passes all six cases, including self-contained checksum verification and refusal of unsafe archive state.

The broader rollback and safety coverage also remains green, including both test-only injected mid-transaction overwrite rollback cases, non-SCSI import-slot refusal before import, shared-volume/refusal paths, mount ownership cleanup, inactive-LV activation restoration, and dry-run immutability.

## Host/platform observed in evidence

The matching static evidence reports:

```text
Proxmox VE: 8.4.0
pve-manager: 8.4.14
kernel: 6.8.12-15-pve
lvm2: 2.03.16-2
```

The operational transcript was produced by the same all-groups invocation and exercised real QEMU/LXC, LVM/LVM-thin, loopback storage, filesystem, mount, import/export, storage-move, VMID-change, network, and whole-VM archive/restore workflows.

## Acceptance conclusion

The previous v3.7.0 run exposed six root causes and passed only 130/137 real cases. v3.7.1 corrects those issues and the fresh 2026-08-22 run passes the complete current definition:

```text
78/78 static/CLI
137/137 real integration
15/15 groups
0 failures
0 skips
0 anomalies
```

No production or test behavior was changed as part of recording this acceptance result; only release-status documentation was updated afterward.
