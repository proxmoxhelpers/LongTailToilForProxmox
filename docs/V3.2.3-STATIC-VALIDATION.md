# v3.2.3 Static Validation

Date: 2026-08-17

v3.2.3 extends both overwrite helpers so `DEST_VMID disk-N` is also valid when no active destination disk currently uses that backing number.

Validated locally:

```text
Project commands                 : 40
Shell files parsed with /bin/sh  : 54
Syntax failures                  : 0
Standalone-runtime failures      : 0
CLI/version failures             : 0
Plan-only test-group failures    : 0

Static suite:
Passed                           : 18
Failed                           : 0
Skipped                          : 0
Anomalies                        : 0
```

The added static contract verifies that both overwrite helpers have an explicit empty-target path, select a first free SCSI slot, keep `delete` harmless when no old disk exists, and retain collision refusal.

The real integration suite now includes:

```text
copy-overwrite empty target
snapshot-overwrite empty target
```

Each verifies exact requested `vm-DESTVMID-disk-N` creation, no `unusedN`/`disk-901` archive, `delete` no-op behavior, and normal data/origin verification.

This document is not a replacement for the real Proxmox acceptance run.

Run:

```sh
./tests/groups/50-copy-snapshot.sh --run --verbose
```

then:

```sh
./tests/run-all-tests.sh --run --verbose
```
