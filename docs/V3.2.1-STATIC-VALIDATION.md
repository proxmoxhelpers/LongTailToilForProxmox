# v3.2.1 Static Validation

Date: 2026-08-17

This patch corrects the overwrite semantics of:

- `create-disk-copy-and-overwrite-disk-on-vm.sh`
- `create-disk-snapshot-and-overwrite-disk-on-vm.sh`

Expected final behavior:

```text
old target:    vm-DEST-disk-N -> vm-DEST-disk-901+  (preserved by default)
replacement:   staged temp    -> vm-DEST-disk-N
```

With `delete`, the archived old LV is removed only after the replacement is attached, verified, and destination state restoration succeeds.

Local validation completed:

```text
Project commands                 : 40
Shell files parsed with /bin/sh  : 54
Syntax failures                  : 0
CLI/version failures             : 0
Standalone-runtime failures      : 0
README wget+chmod entries        : 40
Test-matrix coverage             : 40/40
Plan-only test-group failures    : 0
Broken local Markdown links      : 0

Static suite:
Passed                           : 16
Failed                           : 0
Skipped                          : 0
Anomalies                        : 0
```

The copy/snapshot integration group now contains explicit real-system assertions for:

- copy-overwrite preserving the original destination disk number;
- copy-overwrite archiving the displaced LV to `disk-901+`;
- copy-overwrite `delete`;
- snapshot-overwrite preserving the original destination disk number;
- snapshot-overwrite archiving the displaced LV to `disk-901+`;
- snapshot-overwrite `delete`.

This static record does **not** replace real Proxmox acceptance. Run:

```sh
./tests/groups/50-copy-snapshot.sh --run --verbose
```

and then:

```sh
./tests/run-all-tests.sh --run --verbose
```

before promoting v3.2.1 to the integration-validated baseline.
