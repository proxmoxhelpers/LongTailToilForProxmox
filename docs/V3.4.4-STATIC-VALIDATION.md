# v3.4.4 Static Validation

Date: 2026-08-17

Local validation after the second real-run fixes:

```text
Project commands                 : 42
Integration cases defined        : 86
Shell files parsed with /bin/sh  : 56
Standalone top-level helpers     : 42/42
Static tests passed              : 43
Static tests failed              : 0
Static anomalies                 : 0
Plan-only launcher failures      : 0
```

The static suite includes all previous contracts plus three new real-run regressions:

- all four create helpers size managed source LVs from LVM metadata rather than `blockdev`;
- move/overwrite helpers expose pause-detach preflight for unsafe SCSI controller-removal topologies;
- disposable LVM-thin integration storages advertise LXC `rootdir` support and the network fixture validates its CT config.

A fresh real-host integration run is still required:

```sh
./tests/run-all-tests.sh --run --verbose
```
