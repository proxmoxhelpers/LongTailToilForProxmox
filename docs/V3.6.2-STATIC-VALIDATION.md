# v3.6.2 Static Validation

## Result

Project **v3.6.2** / test suite **v3.0.2** completed the integrated static/CLI group cleanly:

```text
Passed    : 71
Failed    : 0
Skipped   : 0
Anomalies : 0
```

Validation command:

```sh
sh tests/groups/00-static-cli.sh --run --verbose
```

## Coverage

The static suite validates all **82 public commands** for POSIX syntax, `/bin/sh` entry points, standalone packaging, prohibited-language constructs, safe `set -e` patterns, setup-time elevation detection, lifecycle ordering, `--help`/`--version`, dry-run/version argument ordering, exact `.usage` snapshots, README/doc/test-matrix inventory, parser/help agreement for the new command family, source-function documentation, embedded companion synchronization and the existing v3.5.2 storage transaction invariants.

Additional v3.6.2 source contracts verify:

- one synchronized embedded runtime across all 40 new commands;
- LV-UUID-aware physical reference primitives and the status-preserving LVM wrapper;
- source activation restoration plus destination-UUID ownership in new copy/conversion failure cleanup;
- invocation-owned mount/unmount cleanup;
- strict-slot/snapshot/backup/rollback boundaries for direct config editors;
- exact XFS mountpoint/source verification with filesystem-only growth;
- root-owned unpredictable journals and whole-archive remote hash verification;
- self-contained/checksummed VM archives;
- no-`eval` bulk dispatch;
- non-sparse writes for regular-LV destinations;
- grow-only block/filesystem separation;
- repair/flatten rollback contracts.

## Integration boundary

The project now defines **137 real integration cases** across 14 operational groups. They require a disposable Proxmox/LVM environment and were not run in this environment.

The last completed real-host baseline remains **v3.5.1 / suite v2.9.1**, which passed 50 static and 88 real integration cases with zero skips, failures or anomalies. v3.6.2 is therefore **statically validated / release candidate**, not yet real-host integration-validated.
